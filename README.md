# Computer-Vision-Document-Scanner
A document scanning pipeline that automatically detects the largest quadrilateral contour in an image, applies perspective transformation to correct skew and angle, and outputs a flat, scan-like version of the document
